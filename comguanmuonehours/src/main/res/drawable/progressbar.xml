<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        <item
            android:id="@android:id/background"
            android:drawable="@color/progressbar_right">
        </item>
        <item
            android:id="@android:id/progress"
            android:drawable="@color/progressbar_left">
        </item>-->


    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:centerColor="@color/progressbar_right"
                android:endColor="@color/progressbar_right"
                android:startColor="@color/progressbar_right"/>
        </shape>
    </item>
    <item android:id="@android:id/progress"
        >
        <clip>
            <shape>
                <gradient
                    android:endColor="@color/progressbar_left"
                    android:startColor="@color/progressbar_left"/>
            </shape>
        </clip>
    </item>
</layer-list>